var uri = {
    "root": "",
    "api": "",
}

var route = {
    "api": {
        "login": uri.api + '/login',
        "users": uri.api + '/users',
        "user" : uri.api + '/user',
        "profile": uri_api + '/user/profile'
    }
}